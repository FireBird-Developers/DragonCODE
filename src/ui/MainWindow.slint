import { VerticalBox } from "std-widgets.slint";

export component MainWindow {
    
    preferred-height: 100%;
    preferred-width: 100%;
    VerticalBox {
        
        Text {
            text: "Hello from Slint Ui";
            color: red;
            font-size: 50px;
            
        }
    }
}